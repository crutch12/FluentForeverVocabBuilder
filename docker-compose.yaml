version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    # volumes:
      # - .:/app/
    # command: bash -c "pip install -r requirements.txt && python app.py"
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_RUN_PORT=5000
    volumes:
      - web-data:/app/app/temp

  # anki-desktop:
  #   image: mikescherbakov/anki-desktop-docker:latest
  #   container_name: anki-desktop
  #   # ports:
  #       # - "3000:3000"
  #       # - "8765:8765"
  #   volumes:
  #       - anki-data:/config/.local/share
  #   restart: unless-stopped

  tuna:
    # image: yuccastream/tuna:latest
    build:
      context: .
      dockerfile: Dockerfile.tuna
    depends_on:
      - web
    restart: unless-stopped
    env_file:
      - ./.env.tuna
    entrypoint: /usr/local/bin/tuna_http_fix_already_active.sh
    command: http web:5000
    volumes:
      - ./tuna_http_fix_already_active.sh:/usr/local/bin/tuna_http_fix_already_active.sh

volumes:
    anki-data:
    web-data:
